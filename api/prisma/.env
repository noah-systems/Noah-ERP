# Prisma CLI used to rely on this file for DATABASE_URL, but that now lives in
# ../.env so we avoid duplicate definitions that trigger Prisma warnings.
# Keep this file to document the behaviour for developers checking the schema
# directory in isolation.
